# 旧 WordPress 站点路径 → 新站（保留 SEO 权重，仅跨路径跳转，不会自循环）
/offerings/          /services    301
/offerings           /services    301
/offerings/contact/  /contact     301
/offerings/contact   /contact     301

# 重要：不要在本文件添加“本站自身尾斜杠互转”规则（如 /about/ -> /about）。
# 这类规则会与 Cloudflare 对目录资源（about/index.html）的尾斜杠自动处理冲突，
# 造成 “/about -> /about/ -> /about -> …” 的无限重定向（ERR_TOO_MANY_REDIRECTS）。
# Astro 已用 trailingSlash: 'never' 生成无斜杠的规范链接，无需在此再处理尾斜杠。
